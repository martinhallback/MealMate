$(document).ready(function () {


    $('li.nav-item a.nav-link').click(function (e) {
        e.preventDefault();
        var linkText = $(this).text();

        switch (linkText) {
            case 'Sell':
              
              break;
            case 'Contact':
              
              break;
            
            default:
              
          }
    });
    
});